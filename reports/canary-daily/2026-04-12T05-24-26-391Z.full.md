# 项目总结｜WA CRM Finetuned 接管 Canary 日报
> 日期：2026-04-12
> 来源：wa-crm-v2 / auto daily sender

## 今日结论
- 接管配置：`USE_FINETUNED=true`，`AB_RATIO=0.1`。
- 微调模型：`ft:gpt-4.1-mini-2025-04-14:personal:wa-crm-min-522676:DTh7SccC`。
- 最新窗口命中：`provider=finetuned` (id=1427, status=success)。

## 关键指标（最新窗口）
- 窗口定义：`generation_log.id BETWEEN 1416 AND 1427`（12 条）。
- Provider 分布：`openai=10，finetuned=2`。
- Bucket 分布：`openai=10，finetuned=2`。
- 成功率：`12/12 = 100.0%`。
- 平均延迟（openai）：`3619ms`
- 平均延迟（finetuned）：`5811ms`

## 日维度观测（最近 24h）
- 总请求：`1223`
- 总成功：`774`
- Provider 分布：`openai=1209，finetuned=14`
- Provider 成功：`openai=760，finetuned=14`
- Provider 平均延迟：`openai=3307ms，finetuned=2095ms`

## SFT 就绪度
- approved：`488`
- custom：`488`
- scenes：`8`

## SQL 指标口径（日报模板可复用）
```sql
-- 1) 最新窗口 provider 分布（窗口 ID 从日报正文复制）
SELECT provider, COUNT(*) AS c, SUM(CASE WHEN status='success' THEN 1 ELSE 0 END) AS success_count, ROUND(AVG(latency_ms), 0) AS avg_latency_ms
FROM generation_log
WHERE id BETWEEN 1416 AND 1427
GROUP BY provider
ORDER BY c DESC;

-- 2) 最新窗口 AB bucket 分布
SELECT COALESCE(ab_bucket, 'null') AS ab_bucket, COUNT(*) AS c
FROM generation_log
WHERE id BETWEEN 1416 AND 1427
GROUP BY COALESCE(ab_bucket, 'null')
ORDER BY c DESC;

-- 3) 最近 24h provider 维度健康度
SELECT provider, COUNT(*) AS c,
       SUM(CASE WHEN status='success' THEN 1 ELSE 0 END) AS success_count,
       ROUND(AVG(latency_ms), 0) AS avg_latency_ms
FROM generation_log
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 24 HOUR)
GROUP BY provider
ORDER BY c DESC;

-- 4) 最近 24h 总体健康度
SELECT COUNT(*) AS total,
       SUM(CASE WHEN status='success' THEN 1 ELSE 0 END) AS success_count,
       ROUND(AVG(latency_ms), 0) AS avg_latency_ms
FROM generation_log
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 24 HOUR);

-- 5) 最近一条 finetuned 证据行
SELECT id, client_id, provider, model, ab_bucket, status, latency_ms, scene, operator, created_at
FROM generation_log
WHERE provider='finetuned'
ORDER BY id DESC
LIMIT 1;
```

## 下一步建议（明日）
- 继续保持当前 AB 配置观察 24 小时（人工改写率 / 命中率 / 回复采纳率）。
- 若核心指标稳定，按阶梯提升 AB_RATIO（例如 0.2）并复跑同口径 canary。